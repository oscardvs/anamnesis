export const metadata = { title: "Offline - Anamnesis" };

export default function Offline() {
  return (
    <div style={{ padding: "3rem", textAlign: "center" }}>
      <h1>Offline</h1>
      <p>Anamnesis needs to reach your hub to load memory. Reconnect to your tailnet and retry.</p>
    </div>
  );
}
